/**
 * Giveaway Request Validator Foundation
 * Stubs for request payload validation middleware.
 */

export const validateJoinRequest = (req, res, next) => {
  // In Phase 0, passes through to controller stubs (which return 501)
  next();
};

export const validateClaimRequest = (req, res, next) => {
  // In Phase 0, passes through to controller stubs (which return 501)
  next();
};
